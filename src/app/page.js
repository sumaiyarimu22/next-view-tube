import React from "react";
import HeroSection from "./components/HeroSection";

const page = () => {
  return (
    <div>
      <HeroSection title={`LET'S WATCH MOVE TOGETHER`} imgUrl={"/home.svg"} />
    </div>
  );
};

export default page;
