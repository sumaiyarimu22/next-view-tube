import HeroSection from "../components/HeroSection";

const page = () => {
  return (
    <div>
      <HeroSection title={"OUR STORY"} imgUrl={"/about1.svg"} />
    </div>
  );
};

export default page;
